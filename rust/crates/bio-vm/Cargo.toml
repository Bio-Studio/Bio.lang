[package]
name = "bio-vm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "BioLang 解释器（M3）：流注册表 + eval + 内置流"

[dependencies]
bio-syntax = { path = "../bio-syntax" }
bio-core = { path = "../bio-core" }
