[package]
name = "bbb-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "BiuBiuBiu 插件 wasm 核心：Rust 实现的格式化器/词法器（VSCode 插件用）"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
bio-syntax = { path = "../bio-syntax" }
