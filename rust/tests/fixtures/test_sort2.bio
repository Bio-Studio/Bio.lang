program main;

Main {
    void exec() {
        Array a = new Array(4);
        a::set(0, 30); a::set(1, 10); a::set(2, 50); a::set(3, 20);
        CIO::println("before:", a);
        Arrays::sort(a);
        CIO::println("after: ", a);
    }
}
