program main;

Interface Shape {
    double area();
    void draw();
}

Class Circle implements Shape {
    double area() { res 0.0; }
}

Main {
    void exec() {
        Circle c = new Circle();
    }
}
