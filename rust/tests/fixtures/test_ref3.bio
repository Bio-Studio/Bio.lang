program main;

Class Box {
    void take(p &r u int) {
        CIO::println("got:", get p);
    }
}

Main {
    void exec() {
        int x = 42;
        Box b = new Box();
        b::take(&x);
    }
}
