program main;

Class Box {
    void bump(p &w u int) {
        p = get p + 1;
    }
}

Main {
    void exec() {
        int x = 41;
        Box b = new Box();
        b::bump(&x);
        CIO::println("x =", x);
    }
}
