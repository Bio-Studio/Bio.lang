program main;

Main {
    void exec() {
        Array a = new Array(5);
        a::set(0, 30); a::set(1, 10); a::set(2, 50); a::set(3, 20); a::set(4, 40);
        CIO::println("before:", a);
        a::sort();
        CIO::println("after: ", a);
    }
}
