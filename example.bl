program main;

Main {
    void exec() {
        int length = res CIO::readInt("Length: ");
        int[] a = new int[length];
        for (int i = 0; i < n; i++) {
            a[i] = res CIO::readInt(SIO::format("IndeX %d: ", i));
        }
    }
}

