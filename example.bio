program main;

Main {
    void exec() {
        int[] a = new int[5];
        for (int i = 0; i < 5; i++) {
            a[i] = CIO::readInt("");
        }
        for (int i = 0; i < 5; i++) {
            CIO::print(SIO::format("%d ", a[i]));
        }
        CIO::println();
    }
}
